# --- R session state ---
.Rhistory
.RData
.Rproj.user/

# --- Scraped/downloaded data (large, immutable; reproducible by re-running scripts) ---
data/raw/
data/external/

# data/processed/ is tracked (small, primary deliverable).
# club_elo_lookup.csv is 33MB intermediate, reproducible via 04 -- not tracked.
data/processed/club_elo_lookup.csv

# --- Secrets (never commit) ---
kaggle.json
.env
*.pem
secrets/

# --- Local publishing metadata (account/app IDs, never source) ---
app/rsconnect/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.tmp
*.log
